# Base path for deployment (must match backend BASE_PATH)
# Leave empty for root path deployment
VITE_BASE_PATH=

# Note: The dev server automatically reads PORT from the root .env file
# to proxy API requests to the correct backend port (default: 7395)
