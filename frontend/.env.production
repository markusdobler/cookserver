# Production environment variables for frontend build

# Base path for deployment (must match backend BASE_PATH)
# Example: /import for subpath deployment, / for root deployment
VITE_BASE_PATH=/import

# API URL for production (relative to basepath)
# In production, the backend serves both API and static files
VITE_API_URL=
